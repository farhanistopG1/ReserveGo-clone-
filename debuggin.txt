Originally, i was thinking:

Customer
        ↓
Create Dictionary
        ↓
Loop
        ↓
Build Waitlist

But the question was:

Loop over what?

There was nothing to loop over.


The bug was

I never had somewhere to keep the active waitlist



added

This line:

active_waitlist = []

looks tiny.

But architecturally it's huge.

Why?

Because for the first time your organism has

state.



Not every business object should be created inside a function. Some business objects represent the application's current state and therefore must exist independently of any single request.

That is exactly what active_waitlist is.



The Mental Model You Just Built

This is the model I want to remember.

Request

↓

Create Object

↓

Store Object

↓

Return Collection
